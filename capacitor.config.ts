import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.myNewProject",
  appName: "myNewProject",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
